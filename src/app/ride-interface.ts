export interface IRide {
      id: number;
      
      customerId: number;

      driverId: number;

      driverXLocation: number;

      driverYLocation: number;

      destination: number;

      driverName: number;

      driverAccepted: boolean;
}